---
phase: 01-technical-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/user-summary/route.ts
  - src/app/api/payments/create-session/route.ts
  - src/app/api/admin/summary/route.ts
  - src/app/api/payments/stats/route.ts
autonomous: true
requirements: [TECH-01]

must_haves:
  truths:
    - "Nenhuma rota de API define ou hardcoda os valores 1650 ou 35 localmente"
    - "Todas as 4 rotas importam TOTAL_RENTAL e AVISO_PRICE de @/lib/constants"
    - "Os calculos de rentalShare produzem resultados identicos antes e depois do refactor"
  artifacts:
    - path: "src/app/api/user-summary/route.ts"
      provides: "Calculo de rentalShare usando constantes importadas"
      contains: "import { TOTAL_RENTAL, AVISO_PRICE } from"
    - path: "src/app/api/payments/create-session/route.ts"
      provides: "calculateServerAmount e expense_share usando constantes importadas"
      contains: "import { TOTAL_RENTAL, AVISO_PRICE } from"
    - path: "src/app/api/admin/summary/route.ts"
      provides: "Calculo de rentalShare usando constantes importadas"
      contains: "import { TOTAL_RENTAL, AVISO_PRICE } from"
    - path: "src/app/api/payments/stats/route.ts"
      provides: "Calculo de rentalShare usando constantes importadas"
      contains: "import { TOTAL_RENTAL, AVISO_PRICE } from"
  key_links:
    - from: "src/app/api/user-summary/route.ts"
      to: "src/lib/constants.ts"
      via: "import { TOTAL_RENTAL, AVISO_PRICE }"
      pattern: "from \"@/lib/constants\""
    - from: "src/app/api/payments/create-session/route.ts"
      to: "src/lib/constants.ts"
      via: "import { TOTAL_RENTAL, AVISO_PRICE }"
      pattern: "from \"@/lib/constants\""
    - from: "src/app/api/admin/summary/route.ts"
      to: "src/lib/constants.ts"
      via: "import { TOTAL_RENTAL, AVISO_PRICE }"
      pattern: "from \"@/lib/constants\""
    - from: "src/app/api/payments/stats/route.ts"
      to: "src/lib/constants.ts"
      via: "import { TOTAL_RENTAL, AVISO_PRICE }"
      pattern: "from \"@/lib/constants\""
---

<objective>
Centralizar constantes financeiras (TOTAL_RENTAL=1650, AVISO_PRICE=35) eliminando todas as definicoes locais e literais hardcoded nas 4 rotas de API identificadas.

Purpose: Garantir que mudancas futuras de valor sejam feitas num unico lugar (src/lib/constants.ts), eliminando risco de divergencia entre rotas. Per D-01 e D-02.
Output: 4 arquivos de rota refatorados, zero hardcodes restantes.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-technical-foundation/01-CONTEXT.md
@.planning/phases/01-technical-foundation/01-RESEARCH.md

<interfaces>
<!-- Source of truth: src/lib/constants.ts -->
```typescript
export const TOTAL_RENTAL = 1650;
export const AVISO_PRICE = 35;
export const MEAT_PER_PERSON_KG = 0.5;
export const MEAT_PRICE_PER_KG = 40;
export const CHOPP_PER_PERSON_L = 4;
export const TICKET_COST = 2;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded constants in admin/summary and payments/stats</name>
  <read_first>
    - src/lib/constants.ts
    - src/app/api/admin/summary/route.ts
    - src/app/api/payments/stats/route.ts
  </read_first>
  <files>src/app/api/admin/summary/route.ts, src/app/api/payments/stats/route.ts</files>
  <action>
Per D-01 and D-02. Both files follow the same pattern: local `const TOTAL_RENTAL = 1650;` and `const AVISO = 35;` at the top.

For `src/app/api/admin/summary/route.ts`:
1. Add import at line 1 (after existing imports): `import { TOTAL_RENTAL, AVISO_PRICE } from "@/lib/constants";`
2. Delete lines 4-5 (the local `const TOTAL_RENTAL = 1650;` and `const AVISO = 35;`)
3. On line 60, replace `AVISO` with `AVISO_PRICE` in the expression `TOTAL_RENTAL / confirmedCount - AVISO`. The `TOTAL_RENTAL` name stays the same since it matches the export name.

For `src/app/api/payments/stats/route.ts`:
1. Add import at line 1 (after existing imports): `import { TOTAL_RENTAL, AVISO_PRICE } from "@/lib/constants";`
2. Delete lines 4-5 (the local `const TOTAL_RENTAL = 1650;` and `const AVISO = 35;`)
3. On line 99, replace `AVISO` with `AVISO_PRICE` in the expression `TOTAL_RENTAL / confirmedCount - AVISO`.

IMPORTANT per Research Pitfall 1: Do NOT globally replace "AVISO" -- only replace the constant declaration and its usage in the math expression. Other references like variable names `temAviso`, `avisoPaid`, or string literals containing "aviso" must NOT be touched.
  </action>
  <verify>
    <automated>cd "C:/Users/JoãoOliveira/Documents/JV/Plataforma Site Copa/copa-churrasco" && grep -n "const TOTAL_RENTAL\|const AVISO" src/app/api/admin/summary/route.ts src/app/api/payments/stats/route.ts; echo "---"; grep -n "from \"@/lib/constants\"" src/app/api/admin/summary/route.ts src/app/api/payments/stats/route.ts; echo "---"; grep -n "AVISO_PRICE" src/app/api/admin/summary/route.ts src/app/api/payments/stats/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "const TOTAL_RENTAL = 1650" returns ZERO matches in both files
    - grep "const AVISO = 35" returns ZERO matches in both files
    - grep 'from "@/lib/constants"' returns exactly 1 match per file (the import line)
    - grep "AVISO_PRICE" returns at least 1 match per file (in the import and in the calculation)
    - grep "TOTAL_RENTAL / confirmedCount - AVISO_PRICE" returns exactly 1 match per file
  </acceptance_criteria>
  <done>admin/summary/route.ts and payments/stats/route.ts import TOTAL_RENTAL and AVISO_PRICE from @/lib/constants. No local constant definitions remain. Calculation expressions use AVISO_PRICE instead of AVISO.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded constants in user-summary and create-session</name>
  <read_first>
    - src/lib/constants.ts
    - src/app/api/user-summary/route.ts
    - src/app/api/payments/create-session/route.ts
  </read_first>
  <files>src/app/api/user-summary/route.ts, src/app/api/payments/create-session/route.ts</files>
  <action>
Per D-01 and D-02. These files use inline numeric literals instead of named constants.

For `src/app/api/user-summary/route.ts` (per Research Pitfall 2):
1. Add import at top (after existing imports on lines 1-3): `import { TOTAL_RENTAL, AVISO_PRICE } from "@/lib/constants";`
2. On line 144, replace `1650 / confirmedCount - 35` with `TOTAL_RENTAL / confirmedCount - AVISO_PRICE`

For `src/app/api/payments/create-session/route.ts` (per Research Pitfall 3 -- TWO locations):
1. Add import at top (after existing imports on lines 1-4): `import { TOTAL_RENTAL, AVISO_PRICE } from "@/lib/constants";`
2. On line 28 (inside `calculateServerAmount`, case "aviso"), replace `return 35.0;` with `return AVISO_PRICE;`
3. On line 103 (inside case "expense_share"), replace `const rentalShare = 1650 / totalConfirmed - 35;` with `const rentalShare = TOTAL_RENTAL / totalConfirmed - AVISO_PRICE;`

IMPORTANT: Do NOT touch `return 2.0;` on line 31 (bolao case) -- that is TICKET_COST, a different constant, and not in scope for D-01.

After both files are done, run a final verification grep across the entire src/app/api/ directory to confirm zero remaining hardcodes of 1650 or 35 in calculation context.
  </action>
  <verify>
    <automated>cd "C:/Users/JoãoOliveira/Documents/JV/Plataforma Site Copa/copa-churrasco" && echo "=== Remaining hardcodes ===" && grep -rn "= 1650\|1650 /\|= 35\b\|/ 35\b\|35\.0" src/app/api/ || echo "NONE FOUND (good)"; echo "=== Imports ===" && grep -rn 'from "@/lib/constants"' src/app/api/user-summary/route.ts src/app/api/payments/create-session/route.ts; echo "=== Build check ===" && cd "C:/Users/JoãoOliveira/Documents/JV/Plataforma Site Copa/copa-churrasco" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep for "1650" in numeric/calculation context across src/app/api/ returns ZERO matches
    - grep for "= 35" or "- 35" or "35.0" in numeric context across src/app/api/ returns ZERO matches
    - grep 'from "@/lib/constants"' returns 1 match in user-summary/route.ts
    - grep 'from "@/lib/constants"' returns 1 match in create-session/route.ts
    - grep "AVISO_PRICE" in create-session/route.ts returns exactly 2 matches (line 28 aviso case + line 103 expense_share case)
    - npm run build (or npx next build) completes without type errors
  </acceptance_criteria>
  <done>All 4 API routes import TOTAL_RENTAL and AVISO_PRICE from @/lib/constants. Zero hardcoded 1650 or 35 values remain in any API route file. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan is a pure refactor of server-side constants -- no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Constants import path | accept | Constants are server-side only (API routes). Client cannot influence import resolution. Risk is negligible. |
</threat_model>

<verification>
1. `grep -rn "= 1650\|= 35\b\|1650 /\|/ 35\|35\.0" src/app/api/` returns zero matches
2. `grep -rn 'from "@/lib/constants"' src/app/api/` returns exactly 4 matches (one per route file)
3. `npx next build` completes without errors
</verification>

<success_criteria>
All four API routes (user-summary, create-session, admin/summary, payments/stats) import TOTAL_RENTAL and AVISO_PRICE from src/lib/constants.ts. No hardcoded 1650 or 35 values remain in any API route. Build passes cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-technical-foundation/01-01-SUMMARY.md`
</output>
